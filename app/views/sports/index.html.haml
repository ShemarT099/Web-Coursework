%h1 Sports



%table


  %tbody
    - @sports.each do |sport|
      %tr
        %td= sport.title
        .overlay
          %td= image_tag(sport.image_url, class: 'image')


%br

= link_to 'New Sport', new_sport_path
